# MEXC Copy Trader Bot

Telegram бот для копитрейдинга на MEXC Futures с защитой от обнаружения мультиаккаунтинга.

## Возможности

- **Управление несколькими аккаунтами** через Telegram интерфейс
- **Копитрейдинг** с мастер-аккаунта на slave-аккаунты
- **Обработка сигналов** (DOUBLE LONG/SHORT)
- **Защита от мультиаккаунтинга:**
  - Случайные задержки между аккаунтами
  - Разные цены входа
  - Разное плечо
  - Индивидуальные прокси

## Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd mexc-copytrader

# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env, добавьте токен бота и ваш Telegram ID

# Запустите бота
npm start
```

## Настройка

### 1. Telegram бот

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и добавьте в `.env`
3. Узнайте свой Telegram ID через [@getmyid_bot](https://t.me/getmyid_bot)

### 2. MEXC authToken

1. Откройте [futures.mexc.com](https://futures.mexc.com) в браузере
2. Войдите в аккаунт
3. Откройте DevTools (F12) → Application → Cookies
4. Скопируйте значение `u_token`

### 3. Добавление аккаунтов

После запуска бота:
- `/start` → Аккаунты → Добавить аккаунт
- Следуйте инструкциям мастера

## Команды

### Торговля

```
/s TICKER PRICE USD LEVERAGE  - Открыть шорт
/l TICKER PRICE USD LEVERAGE  - Открыть лонг
/cl TICKER [PRICE]            - Закрыть позицию
```

### Примеры

```
/s BTC 42000 100 20   - Шорт BTC на $100, цена 42000, плечо 20x
/l ETH 2200 50 10     - Лонг ETH на $50, цена 2200, плечо 10x
/cl BTC               - Закрыть BTC по рынку
/cl BTC 43000         - Закрыть BTC лимитом по 43000
```

## Сигналы

Просто перешлите боту сообщение с сигналом формата:
```
#TICKER_USDT
Price DEX $X.XX
Price MEXC $X.XX
DOUBLE LONG / DOUBLE SHORT
```

Бот автоматически откроет позиции на всех активных аккаунтах.

### Align-сигналы

```
✅ #TICKER выровнен
Price MEXC $X.XX
```

При получении align-сигнала бот автоматически закрывает позицию.

## Защита от мультиаккаунтинга

### Задержки
- Каждый аккаунт получает случайную задержку в заданном диапазоне
- По умолчанию: 0-1000 мс

### Отклонение цены
- LONG: цена входа случайно увеличивается на 0..+X%
- SHORT: цена входа случайно уменьшается на 0..-X%
- По умолчанию: ±1%

### Разброс плеча
- Плечо выбирается случайно в диапазоне аккаунта
- При одинаковом размере позиции → разная маржа

### Прокси
- Каждому аккаунту можно назначить свой прокси
- Формат: `http://user:pass@ip:port`

## Настройки

Все настройки доступны через меню бота:

- **⏱ Задержки** - мин/макс задержка между аккаунтами
- **📊 Отклонение цены** - % разброса цены входа
- **📈 Разброс плеча** - насколько плечо может отличаться
- **🔄 Режимы копирования** - что копировать
- **📨 Настройки сигналов** - параметры обработки сигналов

## Структура проекта

```
src/
├── bot.ts                 # Главный файл бота
├── config/
│   ├── types.ts           # Типы TypeScript
│   └── storage.ts         # Хранилище конфигурации
├── mexc/
│   ├── client.ts          # MEXC API клиент
│   └── copyTrader.ts      # Движок копитрейдинга
├── telegram/
│   ├── keyboards.ts       # Inline клавиатуры
│   └── handlers/
│       ├── accounts.ts    # Управление аккаунтами
│       ├── settings.ts    # Настройки
│       ├── trading.ts     # Торговые команды
│       └── signals.ts     # Обработка сигналов
└── utils/
    └── helpers.ts         # Вспомогательные функции
```

## Конфигурация

Все настройки сохраняются в `config.json`:

```json
{
  "telegramBotToken": "...",
  "telegramUserId": 123456789,
  "accounts": [...],
  "settings": {
    "delayMinMs": 0,
    "delayMaxMs": 1000,
    "priceDeviationPercent": 1.0,
    "leverageSpread": 10,
    ...
  },
  "contractLimits": {
    "SUBHUB_USDT": 2100,
    ...
  }
}
```

## Миграция из старой версии

Если у вас есть `MEXC_TOKENS` в `.env`, бот автоматически импортирует аккаунты при первом запуске.

## Требования

- Node.js 18+
- TypeScript 5+

## Лицензия

MIT
